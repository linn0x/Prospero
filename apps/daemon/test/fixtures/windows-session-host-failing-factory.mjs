export async function createWindowsSessionHostHandler() {
  // An invalid adapter-start result follows the same fail-closed startup
  // cleanup path without producing a deliberately unhandled fixture promise.
  return null;
}
