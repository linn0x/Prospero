# @prospero/mobile

Expo App(iOS 先行)。W3 时用 `npx create-expo-app@latest` 在此目录生成,加 `expo-dev-client`,并配置 Info.plist(`NSLocalNetworkUsageDescription` / `NSBonjourServices: _prospero._tcp` / 相机权限)。见 `docs/m1-plan.md` W3。
